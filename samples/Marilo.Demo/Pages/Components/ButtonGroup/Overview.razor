@page "/components/button-group"
@page "/components/button-group/overview"
@layout ComponentDemoLayout

<PageSection Title="Overview">
    <DemoSection Title="Basic Usage" Code="@_basicCode">
        <MariloButtonGroup>
            <MariloButton Variant="ButtonVariant.Secondary">Left</MariloButton>
            <MariloButton Variant="ButtonVariant.Secondary">Center</MariloButton>
            <MariloButton Variant="ButtonVariant.Secondary">Right</MariloButton>
        </MariloButtonGroup>
    </DemoSection>
</PageSection>

<PageSection Title="Appearance">
    <DemoSection Title="With Primary" Description="Mix variant styles within a button group to highlight the primary action." Code="@_withPrimaryCode">
        <MariloButtonGroup>
            <MariloButton Variant="ButtonVariant.Primary">Primary</MariloButton>
            <MariloButton Variant="ButtonVariant.Secondary">Secondary</MariloButton>
            <MariloButton Variant="ButtonVariant.Secondary">Secondary</MariloButton>
        </MariloButtonGroup>
    </DemoSection>
</PageSection>

<PageSection Title="Events">
    <DemoSection Title="OnClick" Description="Handle click events on individual buttons within the group." Code="@_clickCode">
        <MariloButtonGroup>
            <MariloButton Variant="ButtonVariant.Primary" OnClick="@(() => _selected = "Left")">Left</MariloButton>
            <MariloButton Variant="ButtonVariant.Secondary" OnClick="@(() => _selected = "Center")">Center</MariloButton>
            <MariloButton Variant="ButtonVariant.Secondary" OnClick="@(() => _selected = "Right")">Right</MariloButton>
        </MariloButtonGroup>
        <p><strong>Selected:</strong> @_selected</p>
    </DemoSection>
</PageSection>

<PageSection Title="Accessibility">
    <AccessibilityInfo
        KeyboardInteractions="@_keyboard"
        AriaAttributes="@_aria"
        ScreenReaderNotes="@_screenReader" />
</PageSection>

@code {
    // Overview
    private const string _basicCode =
        "<MariloButtonGroup>\n" +
        "    <MariloButton Variant=\"ButtonVariant.Secondary\">Left</MariloButton>\n" +
        "    <MariloButton Variant=\"ButtonVariant.Secondary\">Center</MariloButton>\n" +
        "    <MariloButton Variant=\"ButtonVariant.Secondary\">Right</MariloButton>\n" +
        "</MariloButtonGroup>";

    // Appearance
    private const string _withPrimaryCode =
        "<MariloButtonGroup>\n" +
        "    <MariloButton Variant=\"ButtonVariant.Primary\">Primary</MariloButton>\n" +
        "    <MariloButton Variant=\"ButtonVariant.Secondary\">Secondary</MariloButton>\n" +
        "    <MariloButton Variant=\"ButtonVariant.Secondary\">Secondary</MariloButton>\n" +
        "</MariloButtonGroup>";

    // Events
    private string _selected = "Left";

    private const string _clickCode =
        "<MariloButtonGroup>\n" +
        "    <MariloButton Variant=\"ButtonVariant.Primary\" OnClick=\"@(() => _selected = \"Left\")\">Left</MariloButton>\n" +
        "    <MariloButton Variant=\"ButtonVariant.Secondary\" OnClick=\"@(() => _selected = \"Center\")\">Center</MariloButton>\n" +
        "    <MariloButton Variant=\"ButtonVariant.Secondary\" OnClick=\"@(() => _selected = \"Right\")\">Right</MariloButton>\n" +
        "</MariloButtonGroup>\n" +
        "<p><strong>Selected:</strong> @_selected</p>\n\n" +
        "@code {\n" +
        "    private string _selected = \"Left\";\n" +
        "}";

    // Accessibility
    private static readonly (string Key, string Action)[] _keyboard =
    [
        ("Tab", "Moves focus to the next button in the group"),
        ("Shift + Tab", "Moves focus to the previous button in the group"),
        ("Enter", "Activates the focused button"),
        ("Space", "Activates the focused button"),
    ];

    private static readonly (string Attribute, string Description)[] _aria =
    [
        ("role=\"group\"", "Applied to the button group container to indicate related buttons"),
        ("aria-label", "Can be set on the group to describe its purpose"),
        ("aria-disabled=\"true\"", "Applied to individual buttons when disabled"),
    ];

    private static readonly string[] _screenReader =
    [
        "The button group is announced as a \"group\" containing related buttons.",
        "Each button within the group is announced individually with its text content.",
        "Use aria-label on the group to provide context about the grouped actions.",
    ];
}

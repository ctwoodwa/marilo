# Stage 02 — Implement

## Entry
Implement the marilo-drag.ts TypeScript module and IJSInteropDragService .NET interface.

## Output
- `output/marilo-drag.ts` — TypeScript module (wwwroot/js)
- `output/IJSInteropDragService.cs` — .NET service interface (Services)
- `output/JSInteropDragService.cs` — .NET service implementation (Services)

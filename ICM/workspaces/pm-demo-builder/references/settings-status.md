# Settings Status — Pointer

The canonical source of truth for settings area progress is:

```
samples/Marilo.PmDemo/SETTINGS_STATUS.md
```

Do NOT duplicate settings status content here. Read the file above directly.

This reference exists so workspace stages can point to the canonical location without embedding the full path in every CONTEXT.md Inputs table.

# Decision Record — Tick 15 — 2026-04-12T02:20Z

**Authorized by:** User (inline "1,2,3" directive = resolve all open decisions + begin S05 + advance treeview)

## DataGrid Decisions

### M-03: Virtual Scrolling API Shape
**Decision:** `EnableVirtualization` boolean parameter + `VirtualItemHeight` double parameter on `MariloDataGrid`.
**Rationale:** Matches Telerik Blazor ecosystem convention. Additive — no breaking change. Virtual height is required for fixed-height row estimation.
**Unblocks:** Lane F item M-03, any dependent virtual-scrolling demo tasks.

### M-05: GridState Genericization
**Decision:** `GridState<TItem>` with `SortDescriptors` and `FilterDescriptors` as `IList<SortDescriptor>` / `IList<FilterDescriptor>` using `Marilo.DataSource.*` descriptor types.
**Rationale:** Mirrors the gantt-state-shape decision from tick 6 (GanttState<TItem>). Consistent API surface across grid components.
**Unblocks:** Lane F item M-05.

### M-12: Pager Compound Object Shape
**Decision:** `PagerSettings` record with `PageSize` (int), `PageIndex` (int), `TotalCount` (int), `PageSizes` (int[]?). Single `Pager` parameter of type `PagerSettings` on `MariloDataGrid`.
**Rationale:** Clean API — one parameter instead of 4. Follows the compound-object pattern used by SchedulerEditPolicy (tick 8).
**Unblocks:** Lane F item M-12.

### VP-006: Density Parameter
**Decision:** `Density` enum parameter (`Compact`, `Normal`, `Comfortable`) on `MariloDataGrid`. Default = `Normal`. Affects row height, padding, font size via CSS custom properties `--mar-datagrid-density-*`.
**Rationale:** Aligns with FluentUI density token system. Provider-first: the enum maps to CSS custom property values in the provider SCSS.
**Unblocks:** VP-006 task in Lane B.

## Gantt Decisions

### W4-INT-19: Selected State API
**Decision:** Add `SelectedItem` (TItem?), `SelectedItems` (IEnumerable<TItem>?), `SelectedItemsChanged` (EventCallback<IEnumerable<TItem>>) to MariloGantt.
**Rationale:** Mirrors MariloTreeView's existing selection API. Consistent cross-component pattern.
**Unblocks:** W4-INT-19 in Lane L4.

### W4-INT-15: Today Line Template
**Decision:** Add `TodayMarkerTemplate` (RenderFragment?) to MariloGantt. Default rendering = vertical line at DateTime.Today. Template opt-in for customization.
**Rationale:** Additive. No breaking change. Common scheduler/gantt feature across competitors.
**Unblocks:** W4-INT-15 (was in L6).

### W4-INT-16: Milestone Template
**Decision:** Add `MilestoneTemplate` (RenderFragment<TItem>?) to MariloGantt. Coexists with default diamond rendering. Template overrides when provided.
**Rationale:** Additive. No conflict with existing bar/summary templates. Standard customization pattern.
**Unblocks:** W4-INT-16 (was in L7).

## TreeView Decisions

### VP-002: Class Naming Convention
**Decision:** Rename SCSS selectors from `mar-treeitem__*` to `mar-tree-item__*` to match Razor markup output.
**Rationale:** Razor markup is the source of truth (it's what the browser renders). SCSS must match. This is a FluentUI+Bootstrap provider SCSS change, not a Razor change.
**Unblocks:** 7 of 17 treeview VP gaps.

### VP-004: CssProvider Density Extension
**Decision:** Add `TreeViewClass(string? size)` overload to `IMariloCssProvider`. FluentUI implements with density token mapping. Other providers get pass-through stub.
**Rationale:** Additive provider extension. Follows the pattern of existing `DataGridClass()` / `ButtonClass()` overloads.
**Unblocks:** VP-004 (density variants).

## Impact Summary

- **DataGrid:** Lane F fully unblocked (8 items). VP-006 unblocked. Total newly unblocked: 9 tasks.
- **Gantt:** 3 items unblocked. Can now be added to remediation plan waves.
- **TreeView:** 7+1 VP gaps unblocked by naming + density decisions.
- **DataSheet UD-01:** Remains orchestrator-only implementation (separate lane, not in this dispatch).

@* Pass-through wrapper for spec-compatible markup structure.
   Consumers can nest ChartCategoryAxis children inside this wrapper
   or place them directly inside MariloChart (both patterns work). *@

@ChildContent

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}

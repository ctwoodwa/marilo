@inherits MariloComponentBase

<li class="@CombineClasses(CssProvider.NavItemClass(IsActive))" style="@CombineStyles()" @attributes="AdditionalAttributes">
    @if (!string.IsNullOrEmpty(Href))
    {
        <a href="@Href" aria-current="@(IsActive ? "page" : null)">
            @ChildContent
        </a>
    }
    else
    {
        <button type="button" @onclick="OnClick" aria-current="@(IsActive ? "page" : null)">
            @ChildContent
        </button>
    }
</li>

@code {
    [Parameter] public string? Href { get; set; }
    [Parameter] public bool IsActive { get; set; }
    [Parameter] public EventCallback OnClick { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
}

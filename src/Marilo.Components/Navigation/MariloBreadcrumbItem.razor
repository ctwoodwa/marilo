@inherits MariloComponentBase

<li class="@CombineClasses(CssProvider.BreadcrumbItemClass())" style="@CombineStyles()" @attributes="AdditionalAttributes">
    @if (IsActive)
    {
        <span aria-current="page">
            @if (!string.IsNullOrEmpty(Icon))
            {
                <span class="mar-breadcrumb__icon">@IconProvider.GetIcon(Icon, Marilo.Core.Enums.IconSize.Small)</span>
            }
            @if (!string.IsNullOrEmpty(Title))
            {
                @Title
            }
            @ChildContent
        </span>
    }
    else if (!string.IsNullOrEmpty(Href))
    {
        <a href="@Href">
            @if (!string.IsNullOrEmpty(Icon))
            {
                <span class="mar-breadcrumb__icon">@IconProvider.GetIcon(Icon, Marilo.Core.Enums.IconSize.Small)</span>
            }
            @if (!string.IsNullOrEmpty(Title))
            {
                @Title
            }
            @ChildContent
        </a>
    }
    else
    {
        <span>
            @if (!string.IsNullOrEmpty(Icon))
            {
                <span class="mar-breadcrumb__icon">@IconProvider.GetIcon(Icon, Marilo.Core.Enums.IconSize.Small)</span>
            }
            @if (!string.IsNullOrEmpty(Title))
            {
                @Title
            }
            @ChildContent
        </span>
    }
</li>

@code {
    [Parameter] public string? Href { get; set; }
    [Parameter] public bool IsActive { get; set; }
    [Parameter] public string? Title { get; set; }
    [Parameter] public string? Icon { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
}

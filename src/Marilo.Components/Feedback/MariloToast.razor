@inherits MariloComponentBase

<div class="@CombineClasses(CssProvider.ToastClass(Severity))"
     style="@CombineStyles()"
     role="alert"
     aria-live="polite"
     @attributes="AdditionalAttributes">
    @if (Icon != null)
    {
        <span class="mar-toast__icon">@Icon</span>
    }
    <div class="mar-toast__content">
        @if (!string.IsNullOrEmpty(Title))
        {
            <div class="mar-toast__title">@Title</div>
        }
        <div class="mar-toast__message">@Message</div>
    </div>
    @if (ShowCloseButton)
    {
        <button type="button" class="mar-toast__dismiss" aria-label="Dismiss" @onclick="OnDismiss">&times;</button>
    }
</div>

@code {
    [Parameter] public ToastSeverity Severity { get; set; } = ToastSeverity.Info;
    [Parameter] public string Message { get; set; } = string.Empty;
    [Parameter] public string? Title { get; set; }
    [Parameter] public EventCallback OnDismiss { get; set; }
    [Parameter] public bool ShowCloseButton { get; set; } = true;
    [Parameter] public RenderFragment? Icon { get; set; }
}

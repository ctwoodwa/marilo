@* WindowActions — container for WindowAction child components in a MariloWindow. *@

@ChildContent

@code {
    [CascadingParameter] internal MariloWindow? ParentWindow { get; set; }

    [Parameter] public RenderFragment? ChildContent { get; set; }

    protected override void OnInitialized()
    {
        if (ParentWindow is null)
            throw new InvalidOperationException("WindowActions must be placed inside a MariloWindow.");
        ParentWindow.SetUsingChildActions(true);
    }
}

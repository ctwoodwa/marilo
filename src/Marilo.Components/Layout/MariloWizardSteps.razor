@* Pass-through wrapper for spec-compatible markup structure.
   Consumers can nest WizardStep children inside this wrapper
   or place them directly inside MariloWizard (both patterns work). *@

@ChildContent

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}

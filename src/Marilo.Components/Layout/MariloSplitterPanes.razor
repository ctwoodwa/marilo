@* Pass-through wrapper for spec-compatible markup structure.
   Consumers can nest MariloSplitterPane children inside this wrapper
   or place them directly inside MariloSplitter (both patterns work). *@

@ChildContent

@code {
    [Parameter] public RenderFragment? ChildContent { get; set; }
}
